# Container Docker com MinIO (+ MinIO Client (mc))

---

## Primeiro uso (ou instalação limpa)

    git clone https://github.com/fahadkalil/bigdata_datalake_docker.git
    cd bigdata_datalake_docker
    docker compose up -d --pull=always
